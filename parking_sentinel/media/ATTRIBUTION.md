# Generated parking test video

`parking-enforcement-staged-60s.mp4` is a **60-second staged video**, generated with **Gemini Omni 1.1 Flash** (`gemini-omni-1.1-flash`) on September 9, 2026, using a synthetic parking-lot reference image.

It shows a stationary parked-car viewpoint. Ordinary vehicles cross the view first; a white patrol-style hatchback with blue door markings and roof-mounted optical cameras crosses around 42–49 seconds. The video is assembled from generated segments, including a quiet interval before the patrol. Background landmarks are aligned during editing to correct generated camera drift, and the dashboard is held fixed to the reference. The output is labeled as generated footage and encoded as 720p H.264 MP4 at 24 fps.

The setting, plates, and vehicles are synthetic. No private camera footage, real enforcement plate database, or identifying location references were supplied. Generative artifacts remain possible. This staged example does not establish detection accuracy in real parking lots.

The detector receives actual decoded video frames. Scene timing is not supplied to the model or used to trigger a detection. No video generation runs during playback: Gemini image recognition is a separate backend operation.

[Google's Omni video-generation documentation](https://ai.google.dev/gemini-api/docs/omni)
